# Logs
logs
*.log
npm-debug.log*

# Runtime data
pids
*.pid
*.seed

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage

# nyc test coverage
.nyc_output

# Grunt intermediate storage (http://gruntjs.com/creating-plugins#storing-task-files)
.grunt

# node-waf configuration
.lock-wscript

# Compiled binary addons (http://nodejs.org/api/addons.html)
build/Release

# Dependency directories
node_modules
.vscode
jspm_packages

# Optional npm cache directory
.npm

# KHÔNG ignore lockfile. Không có lockfile thì mỗi lần build trên Vercel npm
# resolve lại toàn bộ cây dependency → build không tái lập được, và một bản
# patch mới của dependency gián tiếp có thể làm gãy production bất kỳ lúc nào
# (đã xảy ra: sanitize-html 2.17.6 kéo theo htmlparser2 v12 ESM-only).

# Optional REPL history
.node_repl_history
.next
# .env
# .env.*

.DS_Store
/generated/prisma
.superpowers/

# .env
# .env.development
# .env.production
# .env.staging
