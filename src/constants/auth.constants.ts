export enum UserRole {
  SUPER_ADMIN = 'super_admin',
  BEAM_ADMIN = 'beam_admin',
  HR_ADMIN = 'hr_admin',
  RECONCILER = 'reconciler',
  ACCOUNTANT = 'accountant',
  CONTROLLER = 'controller',
  SALE = 'sale',
  CUSTOMER_SERVICE = 'cs',
}

export enum UserPermission {}
